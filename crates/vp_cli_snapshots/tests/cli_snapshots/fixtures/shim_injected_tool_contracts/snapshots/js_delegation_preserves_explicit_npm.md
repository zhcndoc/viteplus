# js_delegation_preserves_explicit_npm

## `vpt mkdir -p node_modules`


## `vpt cp -r js-cli node_modules/vite-plus`


## `vp env exec --node 22.18.0 --npm 10.5.0 npm --version`


## `vp env exec --node 22.18.0 --npm 10.5.0 vp create`

进入 JS CLI 时，必须让显式选择的 npm 优先于捆绑的 npm

```
JS delegation preserves the explicit Node and npm versions
```
