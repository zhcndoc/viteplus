# explicit_npm_after_partial_path_reset

## `vp env exec --node 22.18.0 --npm 10.5.0 node assert-unix-shim-path.cjs partial 10.5.0`

显式指定的 npm 版本会覆盖继承的版本，并在 PATH 重置后保持有效

```
Node and its tools survive partial PATH
```
