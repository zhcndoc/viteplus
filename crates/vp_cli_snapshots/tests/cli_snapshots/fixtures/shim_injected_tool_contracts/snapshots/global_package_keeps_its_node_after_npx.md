# global_package_keeps_its_node_after_npx

## `vp install -g --node 22.18.0 ./runtime-probe`


## `npx --offline --call injected-runtime-probe`

继承的捆绑 npm 不得替换全局软件包所选择的 Node

```
全局软件包及其 Node 子进程使用 Node <version>
```
