# bundled_npm_ignores_uninherited_system_runtime

## `vp env on npm`


## `vp env exec --node 20.18.0 node assert-unix-shim-path.cjs system`

未继承的 npm/npx 来自所选的 Node，即使 PATH 中存在另一个运行时

```
Node and its tools survive system PATH
```
