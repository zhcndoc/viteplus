# inherited_marker_skips_other_trampoline

## `vp env exec --node 22.18.0 node assert-inherited-path.cjs trampoline`

来自其他安装的 trampoline 不得被误认为是注入的 Node

```
Node selection survives trampoline PATH
```
