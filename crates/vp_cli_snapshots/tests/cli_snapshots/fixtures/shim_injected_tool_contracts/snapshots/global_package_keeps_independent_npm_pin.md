# global_package_keeps_independent_npm_pin

## `vpt json-edit package.json packageManager npm@10.5.0`


## `vp env on npm`


## `vp install -g --node 22.18.0 ./runtime-probe`


## `npx --version`


## `npx --offline --call 'injected-runtime-probe 10.5.0'`

单独固定的 npm 优先于所选 Node 自带的 npm

```
Pinned npm <version> is preserved
The global package and its Node children use Node <version>
```
