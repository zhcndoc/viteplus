# 命令配置帮助

## `vp config -h`

```
VITE+ - 面向 Web 的统一工具链

用法：vp config [选项]

为当前项目配置 Vite+（钩子分发器 + 代理集成）。

选项：
  --hooks-dir <path>  自定义钩子目录（默认为 .vite-hooks，或此克隆中上次使用的目录）
  --no-hooks          跳过钩子分发器安装
  --no-agent          跳过更新编码代理指令
  -h, --help          显示此帮助信息

环境变量：
  VP_GIT_HOOKS=0  跳过钩子分发器安装

文档：https://viteplus.dev/guide/commit-hooks
```

## `vp config --help`

```
VITE+ - 统一的 Web 工具链

用法：vp config [选项]

为当前项目配置 Vite+（钩子分发器 + 代理集成）。

选项：
  --hooks-dir <path>  自定义钩子目录（默认：.vite-hooks，或此克隆中上次使用的目录）
  --no-hooks          跳过钩子分发器安装
  --no-agent          跳过更新编码代理说明
  -h, --help          显示此帮助信息

环境变量：
  VP_GIT_HOOKS=0  跳过钩子分发器安装

文档：https://viteplus.dev/guide/commit-hooks
```

## `vp help config`

```
VITE+ - 面向 Web 的统一工具链

用法：vp config [选项]

为当前项目配置 Vite+（钩子调度器 + 编程代理集成）。

选项：
  --hooks-dir <path>  自定义钩子目录（默认：.vite-hooks，或此克隆中上次使用的目录）
  --no-hooks          跳过钩子调度器安装
  --no-agent          跳过更新编程代理指令
  -h, --help          显示此帮助信息

环境变量：
  VP_GIT_HOOKS=0  跳过钩子调度器安装

文档：https://viteplus.dev/guide/commit-hooks
```
