[[case]]
name = "create_org_bundled_escape_check"
vp = "local"
local-registry = true
steps = [
  { argv = ["vp", "create", "@your-org", "--no-interactive"], comment = "`../outside` path rejected at schema-validation, before any tarball fetch", continue-on-failure = true },
]
