# command_pm_approve_builds_pnpm11

## `vp pm approve-builds --all`

转发 pnpm approve-builds --all（没有需要批准的内容）

```
没有等待批准的软件包
```

## `vp pm approve-builds esbuild !core-js`

pnpm 11.x 支持 `!pkg` 拒绝语法——作为位置参数转发

```
没有等待批准的软件包
```
