# command_env_which_nvmrc

## `vp env exec node --version`

请先确保已安装 Node.js

```
<version>
```

## `vp env which node`

核心工具 - 显示从 .nvmrc 解析出的 Node.js 二进制文件路径

```
VITE+ - Web 的统一工具链

<home>/.vite-plus/js_runtime/node/<version>/bin/node
  版本:      20.18.0
  来源:      <workspace>/.nvmrc
```
