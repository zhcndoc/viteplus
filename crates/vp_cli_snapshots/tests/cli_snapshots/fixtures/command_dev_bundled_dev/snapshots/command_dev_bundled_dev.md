# command_dev_bundled_dev

## `vp dev --host 127.0.0.1 --port 0`

启用 experimental.bundledDev 的 dev server 启动并提供 bundled dev client

**退出代码：** 130

**→ expect-milestone：** `dev-server:ready`

```

  VITE+ <version>

  ➜  Local:   http://127.0.0.1:<port>/
  ➜  press h + enter to show help
```

**← write-key：** `ctrl-c`

```

  VITE+ <version>

  ➜  Local:   http://127.0.0.1:<port>/
  ➜  press h + enter to show help

```
