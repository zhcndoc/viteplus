# build_ssr_entry_is_explicit

`--ssr` 选项表示明确的命令意图。Vite+ 在当前目录中运行该命令。

## `vp build --ssr src/server.js`

```
VITE+ - The Unified Toolchain for the Web

✓ 2 modules transformed.
computing gzip size...
dist/server.mjs  <size> kB │ gzip: <size> kB

✓ built in <duration>
```
