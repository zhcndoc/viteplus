# global_only_commands_require_global_cli

## `vp env current`

环境管理需要全局 vp 二进制文件

**退出代码：** 2

```
error: The `env` command is only available in the global `vp` CLI. See https://viteplus.dev/guide/ to install it, then run the same command via the global `vp` binary.
```

## `vp upgrade --check`

自我升级需要全局 vp 二进制文件

**退出代码：** 2

```
error: The `upgrade` command is only available in the global `vp` CLI. See https://viteplus.dev/guide/ to install it, then run the same command via the global `vp` binary.
```

## `vp implode --yes`

自我移除需要全局 vp 二进制文件

**退出代码：** 2

```
error: The `implode` command is only available in the global `vp` CLI. See https://viteplus.dev/guide/ to install it, then run the same command via the global `vp` binary.
```
