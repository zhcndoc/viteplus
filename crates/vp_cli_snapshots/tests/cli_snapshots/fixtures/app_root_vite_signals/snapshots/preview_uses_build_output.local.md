# preview_uses_build_output

声明的 Vite `build` 字段是 `vp preview` 的根意图信号。

## `vpt cp configs/preview-output.ts vite.config.ts`


## `vp preview`

**→ 预期里程碑：** `preview-server:ready`

```
  ➜  Local:   http://127.0.0.1:<port>/
  ➜  press h + enter to show help
```

**← 写入行：** `q`

```
  ➜  Local:   http://127.0.0.1:<port>/
  ➜  press h + enter to show help
q
```
