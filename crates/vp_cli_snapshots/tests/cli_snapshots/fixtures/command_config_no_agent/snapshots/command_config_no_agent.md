# command_config_no_agent

## `git init`


## `vp config --no-agent`

应安装钩子分发器，但跳过代理指令更新

```
```

## `git config --local core.hooksPath`

应为 .vite-hooks/_

```
.vite-hooks/_
```

## `vpt grep-file AGENTS.md 'OUTDATED CONTENT'`

代理必须保持不变（过时标记仍然存在）

```
AGENTS.md: found "OUTDATED CONTENT"
```
