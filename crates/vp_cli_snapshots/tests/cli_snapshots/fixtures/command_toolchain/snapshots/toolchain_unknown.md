# toolchain_unknown

## `vp toolchain rollup`

可读输出指向 package graph

**退出代码：** 1

```
error: `rollup` is not in the Vite+ toolchain
hint: run `vp why rollup` to show project dependencies
```

## `vp toolchain rollup --json`

JSON 输出不包含可读提示

**退出代码：** 1

```
error: `rollup` is not in the Vite+ toolchain
```
