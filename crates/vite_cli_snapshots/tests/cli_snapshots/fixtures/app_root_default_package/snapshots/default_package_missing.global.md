# default_package_missing

defaultPackage 指向缺失目录时，会在任何工作区查找之前报错。

## `cd missing && vp build`

**退出代码：** 1

```
VITE+ - The Unified Toolchain for the Web

error: defaultPackage points to a missing directory: ./packages/nope
```
