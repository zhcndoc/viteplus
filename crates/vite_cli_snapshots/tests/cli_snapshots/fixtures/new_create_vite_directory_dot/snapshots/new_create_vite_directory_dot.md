# 新建 vite 目录 dot

## `vpt mkdir -p my-app`

在当前目录中创建 Vite 应用


## `cd my-app && vp create vite:application --no-interactive --directory .`


## `vpt stat-file my-app/package.json --assert file`

在 my-app 中使用 --directory . 创建

```
my-app/package.json: 文件
```
