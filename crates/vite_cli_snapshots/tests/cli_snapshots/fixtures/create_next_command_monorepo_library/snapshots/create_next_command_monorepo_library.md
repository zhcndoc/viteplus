# 创建_next_command_单仓库_库

## `vp create vite:library --no-interactive`

monorepo：下一条命令应建议使用 vp run

```

使用默认包名：vite-plus-library
◇ 已脚手架生成 packages/vite-plus-library，使用 TypeScript 库
• Node <version>  pnpm <version>
→ 下一步：cd packages/vite-plus-library && vp run
```
