[[case]]
name = "exit_code"
vp = "local"
steps = [
  { argv = ["vp", "run", "script1"], comment = "script1 run, create the cache and should be success", continue-on-failure = true },
  { argv = ["vp", "run", "script1"], comment = "script1 should hit the updated cache", continue-on-failure = true },
  { argv = ["vp", "run", "script2"], comment = "script2 should be failure and not cache", continue-on-failure = true },
  { argv = ["vp", "run", "script2"], comment = "script2 should be failure and not cache", continue-on-failure = true },
]
