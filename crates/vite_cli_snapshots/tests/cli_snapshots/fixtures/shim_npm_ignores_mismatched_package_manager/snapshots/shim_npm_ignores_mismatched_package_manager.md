# shim_npm_忽略不匹配的包管理器

## `vp env exec node --version`

请先确保已安装 Node.js

```
<version>
```

## `npm --version`

npm shim 忽略不匹配的 pnpm@10.19.0 packageManager，并使用项目 Node 的 npm（而不是 10.19.0）

```
<version>
```

## `npx --version`

npx shim 同样会忽略不匹配的 packageManager（不是 10.19.0）

```
<version>
```
