[[case]]
name = "command_install_shortcut"
vp = "local"
steps = [
  { argv = ["vp", "run", "install"], comment = "install shortcut", continue-on-failure = true },
  { argv = ["vp", "run", "install"], comment = "install shortcut hit cache", continue-on-failure = true },
]
