# 安装快捷方式命令

## `vp run install`

安装快捷方式

```
$ vp install ⊘ 缓存已禁用

依赖项：
 tslib 2.8.1

使用 pnpm <version> 在 <duration> 内完成
```

## `vp run install`

安装快捷方式命中缓存

```
$ vp install ⊘ 缓存已禁用
锁定文件已是最新，跳过解析步骤
已是最新

使用 pnpm <version> 在 <duration> 内完成
```
