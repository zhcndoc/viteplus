# shim_npm 使用包管理器版本

## `vp env exec node --version`

请先确保已安装 Node.js

```
<version>
```

## `npm --version`

npm shim 解析固定的 packageManager 版本（10.9.4）

```
<version>
```

## `npx --version`

npx shim 解析固定的 packageManager 版本（10.9.4）

```
<version>
```
