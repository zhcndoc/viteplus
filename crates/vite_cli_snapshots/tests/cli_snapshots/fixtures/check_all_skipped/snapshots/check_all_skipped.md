# 检查全部已跳过

## `vp check --no-fmt --no-lint`

**退出代码：** 1

```
错误：未启用任何检查

若只进行类型检查，请在 vite.config.ts 中启用 `lint.options.typeCheck`；移除 `--no-fmt`/`--no-lint` 标志，或在 vite.config.ts 中重新启用 `check.fmt`/`check.lint`。
```
