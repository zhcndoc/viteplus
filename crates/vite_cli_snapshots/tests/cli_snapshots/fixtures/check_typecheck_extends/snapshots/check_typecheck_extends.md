# 检查 typecheck extends

## `vp check --no-fmt --no-lint`

```
通过：在 2 个文件中未发现类型错误（<duration>，<n> 个线程）
```

## `vp check --no-lint`

```
通过：3 个文件的格式均正确（<duration>，<n> 个线程）
通过：在 2 个文件中未发现类型错误（<duration>，<n> 个线程）
```
