[[case]]
name = "cache_scripts_enabled"
vp = "local"
steps = [
  { argv = ["vp", "run", "hello"], comment = "first run should be cache miss", continue-on-failure = true },
  { argv = ["vp", "run", "hello"], comment = "second run should be cache hit", continue-on-failure = true },
]
