{
  "env": {},
  "serial": true,
  "ignoredPlatforms": ["win32"],
  "commands": [
    "cd npm-global-dot-pkg && npm install -g . # Should install and create link",
    "test -L $VP_HOME/bin/npm-global-dot-cli && echo 'link created' # Link should exist",
    "npm-global-dot-cli # Should be callable via the link",
    "rm -f $VP_HOME/bin/npm-global-dot-cli # Cleanup link",
    "npm uninstall -g npm-global-dot-pkg # Cleanup npm install"
  ]
}
