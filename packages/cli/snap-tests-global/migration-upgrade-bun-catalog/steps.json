{
  "env": {},
  "commands": [
    "vp migrate --no-interactive # existing bun Vite+ catalog WORKSPACE gains a direct vite edge for bun#8406 (bun resolves catalog: only inside a workspace)",
    "cat package.json # the direct vite edge is catalog: (resolving through the bun catalog), not a concrete alias"
  ]
}
