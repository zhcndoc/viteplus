> vp env current --json > current.json && node -e "const info = require('./current.json'); if (info.package_manager?.name !== 'npm' || info.package_manager?.version !== '10.9.4' || info.package_manager?.source !== 'packageManager') process.exit(1); console.log('current reports npm packageManager')"
current reports npm packageManager

> vp env which npm > which.txt && node -e "const text = require('node:fs').readFileSync('which.txt', 'utf8'); if (!text.includes('Package:') || !text.includes('npm@10.9.4') || !text.includes('package.json')) process.exit(1); console.log('which reports npm packageManager')"
which reports npm packageManager

> vp env which npx > which-npx.txt && node -e "const text = require('node:fs').readFileSync('which-npx.txt', 'utf8'); if (!text.includes('Package:') || !text.includes('npm@10.9.4') || !text.includes('package.json')) process.exit(1); console.log('which reports npx packageManager')"
which reports npx packageManager
