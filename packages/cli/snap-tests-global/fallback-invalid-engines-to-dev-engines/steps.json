{
  "ignoredPlatforms": ["win32"],
  "commands": [
    "vp exec node -e \"console.log(process.version)\" # Should use devEngines.runtime 22.18.0, not LTS",
    "vp env which node # Should show devEngines.runtime source"
  ]
}
