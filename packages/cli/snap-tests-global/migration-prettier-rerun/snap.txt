> vp migrate --no-interactive # should detect vite-plus + prettier and auto-migrate prettier
◇ Updated . to Vite+ <semver>
• Node <semver>  pnpm <semver>
• Dependencies:
    vite-plus  latest → <semver>
    vite              → <semver>
• Package manager settings configured
• Skipped editor, hooks, and lint setup. Run `vp migrate --full` to apply them.

> cat package.json # check prettier removed from devDependencies and scripts rewritten
{
  "name": "migration-prettier-rerun",
  "scripts": {
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "<semver>",
      "onFail": "download"
    }
  }
}

[1]> test ! -f .prettierrc.json # check prettier config is removed
[1]> cat vite.config.ts # check oxfmt config merged into vite.config.ts
cat: vite.config.ts: No such file or directory
