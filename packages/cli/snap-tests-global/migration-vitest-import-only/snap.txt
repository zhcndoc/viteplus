> vp migrate --no-interactive # ordinary vitest imports should migrate without retaining direct vitest
◇ Migrated . to Vite+ <semver>
• Node <semver>  pnpm <semver>
• 2 config updates applied, 1 file had imports rewritten

> cat package.json # direct dependency and shared pin should be removed
{
  "name": "migration-vitest-import-only",
  "scripts": {
    "test": "vp test",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "<semver>",
      "onFail": "download"
    }
  }
}

> cat example.spec.ts # source import should use the Vite+ public surface
import { expect, it } from 'vite-plus/test';

it('works', () => {
  expect(true).toBe(true);
});

> cat pnpm-workspace.yaml
catalog:
  vite: npm:@voidzero-dev/vite-plus-core@<semver>
  vite-plus: <semver>
overrides:
  vite: 'catalog:'
peerDependencyRules:
  allowAny:
    - vite
  allowedVersions:
    vite: '*'
