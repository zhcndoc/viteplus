> vp migrate --no-interactive # existing Vite+ project: a Node pin below the supported range is preserved, not raised (native binding supports Node >=20)
◇ Updated . to Vite+ <semver>
• Node <semver>  pnpm <semver>
• Dependencies:
    vite-plus  <semver> → <semver>
    vite              → <semver>
• Package manager settings configured

> cat .node-version # stays 24.3.0
24.3.0

> cat package.json # engines.node stays 24.x and devEngines.runtime node stays ^24 (preserved, not raised)
{
  "name": "migration-preserve-below-policy-node-pins",
  "devDependencies": {
    "vite": "catalog:vite-stack",
    "vite-plus": "catalog:vite-stack"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "<semver>",
      "onFail": "download"
    },
    "runtime": [
      {
        "name": "node",
        "version": "^24"
      }
    ]
  },
  "engines": {
    "node": "24.x"
  }
}

> cat pnpm-workspace.yaml # vite-stack catalog updated to the migration target
packages:
  - .

catalogs:
  vite-stack:
    vite: npm:@voidzero-dev/vite-plus-core@<semver>
    vite-plus: <semver>
overrides:
  vite: catalog:vite-stack
peerDependencyRules:
  allowAny:
    - vite
  allowedVersions:
    vite: '*'
