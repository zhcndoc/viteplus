> chmod +x fix-baseurl.mjs # stub the baseUrl fixer so the dlx stays offline
> vp migrate --full --no-interactive # existing Vite+ project: upgrade plus the full setup
◇ Updated . to Vite+ <semver>
• Node <semver>  pnpm <semver>
• Dependencies:
    vite-plus  <semver> → <semver>
    vite              → <semver>
• 2 config updates applied
• Node version manager file migrated to .node-version
• Package manager settings configured

> cat package.json # vite-plus version upgrade is applied
{
  "name": "migration-upgrade-setup-full-pnpm",
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "<semver>",
      "onFail": "download"
    }
  },
  "scripts": {
    "prepare": "vp config"
  }
}

> cat .node-version # .nvmrc is migrated to .node-version by the full setup
20.19.0

> test ! -f .nvmrc && echo nvmrc-removed # the original .nvmrc is removed
nvmrc-removed

> cat tsconfig.json # tsconfig baseUrl is removed by the full setup
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext"
  }
}
