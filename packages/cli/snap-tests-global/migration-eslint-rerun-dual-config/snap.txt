> vp migrate --no-interactive # should detect vite-plus + eslint and auto-migrate eslint
◇ Updated . to Vite+ <semver>
• Node <semver>  pnpm <semver>
• Dependencies:
    vite-plus  latest → <semver>
    vite              → <semver>
• Package manager settings configured
• Skipped editor, hooks, and lint setup. Run `vp migrate --full` to apply them.

> cat package.json # check eslint removed and scripts rewritten
{
  "name": "migration-eslint-rerun-dual-config",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "<semver>",
      "onFail": "download"
    }
  }
}

[1]> test ! -f eslint.config.mjs # check flat config is removed
[1]> test ! -f .eslintrc # check legacy config is also removed
[1]> cat vite.config.ts # check oxlint config merged into vite.config.ts
cat: vite.config.ts: No such file or directory
