{
  "serial": true,
  "ignoredPlatforms": ["win32"],
  "commands": [
    "vp env default 22.18.0 # Set global default to 22.18.0",
    "vp run check-node # Should also use 22.18.0",
    "vp exec node -e \"console.log(process.version)\" # Should also use 22.18.0",
    "vp env which node # Should show 22.18.0 from 'default' source"
  ],
  "after": ["vp env default lts # Restore default to LTS"]
}
