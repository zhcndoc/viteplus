{
  "commands": [
    {
      "command": "vp build",
      "ignoreOutput": true
    },
    "cat dist/index.html | grep 'lazy-plugin-injected' # async vitePlugins() should apply plugins during build"
  ]
}
