{
  "ignoredPlatforms": [{ "os": "linux", "libc": "musl" }],
  "commands": [
    "vp lint --threads=1 src/nuxt.spec.ts src/unit.spec.ts # bare Vitest imports are exempt in this Nuxt package; the non-config Vite import is preserved (issue #2004); @vitest/browser (not upstream Vitest) still fails",
    "vp lint --threads=1 --fix src/nuxt.spec.ts src/unit.spec.ts # fix @vitest/browser without touching upstream Vitest or the preserved Vite import",
    "cat src/nuxt.spec.ts",
    "cat src/unit.spec.ts",
    "vp lint --threads=1 src/nuxt.spec.ts src/unit.spec.ts # confirm the package-level compatible result is clean"
  ]
}
